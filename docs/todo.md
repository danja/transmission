# TODO

- Add a persisted song timeline with scheduled MIDI clips and control-thread
  editing, compiled into fixed-capacity callback event storage.
- Add persisted VST3 parameter, bypass, send, and master-gain automation with
  bounded sample-offset delivery to the native engine.
- Add a deterministic capture/freeze path for MIDI generator output.
